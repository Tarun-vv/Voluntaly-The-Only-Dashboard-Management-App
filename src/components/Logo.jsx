function Logo({width}) {
  return (
    <div>
      <img
        src="../../public/hero.png"
        alt=""
        style={{ width: `${width}%`, marginBottom: "30px", marginRight: "auto" }}
      />
    </div>
  );
}

export default Logo;
